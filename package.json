{
    "name": "support-ai-app-root",
    "version": "1.0.0",
    "description": "Root script for AI Customer Support App",
    "scripts": {
        "build": "cd client && npm run build",
        "start": "concurrently \"npm run server\" \"npm run client\"",
        "server": "cd server && npm run dev",
        "client": "cd client && npm run dev",
        "install-all": "npm install && cd server && npm install && cd ../client && npm install && npm run build"
    },
    "dependencies": {
        "concurrently": "^8.2.2"
    }
}